# Portfolio Website Repository

GitHub repository:

- https://github.com/hsankc/Portfolio

Purpose:

- Main source repository for `hasankasikci.com.tr`
- Next.js portfolio implementation
- Vercel deployment source
- Public website code and content integration

Local archive:

- `PortfolioContent` is the organized content archive.
- Keep original certificates, proof images and private files out of public deployment unless explicitly approved.
- Use small public previews for certificates and leadership proof.

Implementation note:

- Before building the site, check whether the GitHub repository already has files.
- If it is empty, this workspace can be initialized and connected to the remote.
- If it already has code, clone or pull it first, then merge `PortfolioContent` carefully.

